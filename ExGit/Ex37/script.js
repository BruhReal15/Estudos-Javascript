window.addEventListener("load", function(e){
    var div = document.getElementById("resultado")
    let deuses = ['Aegir', 'Aud', 'Balder']

    div.innerHTML = `O tamanho do vetor é ${deuses.length}`
})